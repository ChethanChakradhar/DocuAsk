{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "cd rag-system && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
